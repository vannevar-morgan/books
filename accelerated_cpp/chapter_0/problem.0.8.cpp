#include <iostream>

int main()
{
  // This is a comment that extends over several lines
  // by using // at the beginning of each line instead of using /*
  // or */ to delimit comments.
  std::cout << "Does this work?" << std::endl;
  return 0;
}

//
// Yes this works.  Multiline comments using /* */ can't be nested such as
// in problem_0_7.  But using single line comments on each line is not a problem.
//
